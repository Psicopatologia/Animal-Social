<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
    <a href="/" class="navbar-brand">Animal Social</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <% if (!user) { %>
            <li class="nav-item">
                <a class="nav-link <%if(title=='Signin') {%>active<%}%>" href="/signin">
                    signin
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <% if(title == 'Signup') {%>active<%}%>" href="/signup">
                    signup
                </a>
            </li>
            <% } else {%>
            <li class="dropdown nav-item" >
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                        data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= user.name %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item <% if(title == 'Profile') {%>active<%}%>" href="/u/<%=user.userName%>">
                            Profile
                        </a>
                        <a class="dropdown-item <% if(title == 'Business') {%>active<%}%>" href="/business">
                            Business
                        </a>
                        <a class="dropdown-item <% if(title == 'Proile') {%>active<%}%>" href="/u/<%=user.userName%>">
                            Settings
                        </a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="/logout">Logout</a>
                    </div>
                    <% }%>
            </li>
        </ul>
    </div>
</nav>