<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a href="/" class="navbar-brand">Animal Social</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <% if (!user) { %>
            <li class="nav-item">
                <a class="nav-link <%if(title=='Signin') {%>active<%}%>" href="/signin">
                    signin
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link <% if(title == 'Signup') {%>active<%}%>" href="/signup">
                    signup
                </a>
            </li>
            <% } else {%>
            <li class="nav-item">
                <a class="nav-link <% if(title == 'Profile') {%>active<%}%>" href="/u/<%=user.userName%>">
                    Profile
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
            </li>
            <% }%>
        </ul>
    </div>
</nav>